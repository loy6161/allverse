# Unity/VRC側からの返答②：本番URL設定完了・座標吸収なし

作成: 2026-07-30 ／ 発行元: Unity/VRChat側開発チャット（VERSE CITY2025）
宛先: ブラウザ版ALLVERSE開発チャット。`HANDOFF_UNITY_2.md` への返答。

---

## 返事（§6の4項目）

### 1. Workerの環境変数に本番URLを設定したか → ✅ 設定済み（2026-07-30から）

- `PRESENCE_URL = https://allverse.onrender.com/api/presence.json` をWorkerにデプロイ済み
- **いつから流れるか＝今すぐ**。Workerの `/v1/live.json` が5秒ごとにそちらのpresenceを取り込み、
  `web[]` として同梱する形が本番稼働中
- 稼働確認済み: そちらの `/api/status`（commit `cf346ba`）→ Workerが取得 → 現在 `web: 0名`（＝今ブラウザ側に誰もいない状態を正しく反映）、`webTs` は最新。ブラウザから入場すれば `web[]` に載る
- Renderのスリープ復帰（コールドスタート）は、Worker側の4秒キャッシュ＋取得失敗時の直近キャッシュ返却で吸収する実装のまま。復帰の数十秒間はワールド側が一時的に「presenceなし」になるだけで、復帰後は自動で流れる

### 2. 座標オフセット吸収を実装していたか → **いいえ（実装していない。外す作業は不要）**

- Worker側は `x`/`z`/`r` を**一切加工せず素通し**している（元からオフセットも回転も掛けていない）
- ワールド（Udon）側の「ブラウザ勢を実際に置くギミック」は**まだ実装していない**段階なので、
  そこにも吸収処理は存在しない。**二重ズレの心配はなし**
- 今後こちらがプロキシ配置を実装する際は、いただいた値を **`transform.position = (x, 床Y, z)` /
  Y回転 = r** としてそのまま使う。設計メモにも「オフセット禁止」を明記した
- 撤回いただいた `PRESENCE_SPEC.md` v0.3 §2.3 の吸収前提は、こちらも破棄で認識合わせOK

### 3. 結合テストの段取り → **こちらの配置ギミックが未実装のため、視覚照合は次段階で**

- 現状こちらは Phase 0（Worker疎通・データ経路の確立）まで。**ブラウザ勢をアバターとして描画する
  ギミックはこれから**（観客NPCプールに「歩行モード」を足す形で実装予定）
- したがって「入場して見える状態にして向きを照合」は、**プロキシ配置を実装した後に合同テスト回**を
  設けたい。日程はこちらの実装が乗ったら声をかけます
- データ経路（位置・向き・アバター見た目 `av`）が正しく届いていることは上記1で確認済みなので、
  残るは「置いてみて向きが合っているか」だけ。いただいた変換式（X軸と回転が符号反転）は把握した。
  **左右反転が起きるとすればここが原因になりやすい**ので、合同テストで最優先に見る

### 4. チャット吹き出し（5-1 `web[].c`）をやるか → **当面やらない（位置連携を先に通す）**

- loyさんの方針「まず位置、チャットはその後」に合わせ、サーバー側 `ENABLE_CHAT_FIELD` は
  **OFFのままでお願いします**
- こちら側の吹き出しギミックも今は作らない。位置連携が本番で安定したら、こちらから相談します

---

## 補足（そちらへの連絡）

- 方向②（VRC→ブラウザ）当面なし・`/api/vrc-presence` 未実装のままでOK、認識一致
- `web[]` 上限はそちら60人・こちらのNPCプールは100体なので、人数面の取りこぼしはなし
- presenceのスキーマ `v=1` 凍結でお願いしたい件、了解。変える時は事前相談する
- clubVERSEの客席矩形（X −234〜−196 / Z −88.4〜−66.9）はこちらの配置実装で範囲チェックに使わせてもらう

次アクション: こちら＝プロキシ配置ギミックの実装 → 実装できたら合同テスト回の打診。
