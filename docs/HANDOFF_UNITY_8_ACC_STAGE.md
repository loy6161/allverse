# 申し送り⑧: アクセサリーの複数付け と ステージ登壇

作成: 2026-08-04 ／ 発行元: ALLVERSE WEB側 ／ 宛先: Unity/VRChat側（VERSE CITY2025）
種別: **事前共有＋Udon側の作業依頼**（実装前に出しています。同時に進められます）

---

## 一言でいうと

**2026-08-03 の本番テストで出たテストユーザーの要望**から、そちらに関係するものが2つあります。

1. **アクセサリーを複数付けられるようにする** → `av.ac` に**複数入るようになります**
2. **管理人とVIPがステージに上がれるようにする** → **客席の外の座標が流れます**

**どちらも、そちらが何もしなくても壊れません。** ただし②は**放置すると見た目が破綻する**ので、
できれば対応してほしいです（理由は下記）。

---

## ① アクセサリーの複数付け（`av.ac`）

### 何が変わるか

いまは1人1つでした。

```json
"av": { "ac": "halo" }
```

これが、**`+` でつないだ複数**になることがあります。

```json
"av": { "ac": "wing+halo" }
```

### 決めたこと

| | |
|---|---|
| 区切り | **半角プラス `+`** |
| 最大 | **3つまで**（それ以上はこちらで切ります） |
| 順番 | **不定**。`"wing+halo"` と `"halo+wing"` は同じ意味 |
| 1つのとき | **いままでどおり `"halo"`**（`+` は付きません） |
| 無しのとき | **いままでどおり `"none"`** |
| 未知のid | **無視**してください（他のidは活かす） |

### なぜ「新しい項目を足す」ではなく「同じ項目に詰める」のか

**presence の契約（v=1）を触りたくないため**です。`ac` は文字列のままなので、
**形は1バイトも変わりません。版上げも要りません。**

### そちらが何もしなかった場合

`"wing+halo"` は**知らないid**として `none` 扱いになります（返答⑥で決めた通り）。
つまり**複数付けた人だけ、VRCではアクセサリー無し**に見えます。壊れはしません。

**1つだけ付けている人は今までどおり**なので、大多数は影響を受けません。

### お願い

**`ac` を `+` で分割して、それぞれ既存の処理にかけてください。** それだけです。

```
// 疑似コード
foreach (id in ac.Split('+')) { AttachAccessory(id); }   // 知らないidは無視
```

排他の考え方はこちらで揃えます（**メガネとサングラスは同時に付きません**）。
そちらで排他を作り込む必要はありません。

---

## ② ステージ登壇（★こちらは対応してほしいです）

### 何が変わるか

**管理人とVIPだけ、ステージの上に立てるようにします。**
イベント設定でON/OFFできるようにするので、常に起きるわけではありません。

その結果、**いままで客席の矩形の中にしか来なかった `x`/`z` が、その外に出ます。**

### 座標の範囲（VRCワールド座標・実測から換算）

| | いままで（客席） | 追加される（ステージ） |
|---|---|---|
| X | 約 **-234 〜 -196** | 約 **-224 〜 -204** |
| Z | 約 **-88 〜 -67** | 約 **-102 〜 -90** |

※ ブラウザ側の実測値（歩ける範囲 `minX:-13, maxX:25, minZ:-16.5, maxZ:5`／
ステージ前端 z=-18.07・背面 z=-30）を、既存の換算式
（`X = -209 - x` ／ `Z = z - 71.91`）で変換したものです。

### ★ 高さの問題（ここが本題）

**presence には高さ(Y)がありません。** `x`/`z`/`向き` だけです。

そのため、**そちらがいまの実装のままだと、ステージに上がった人は
「ステージの中に埋まる」か「宙に浮く」**ことになります。
clubVERSE のステージ天面は**床から約1.4m**上がっているためです。

**お願い**: **その座標の「床の高さ」に合わせてプロキシを置いてください。**
真下にレイを飛ばして床を拾う、で構いません。
そちらのワールドの床の高さは、そちらが正確に知っている情報なので、
**こちらから高さを送るより、そちらで解決してもらう方が確実**だと考えました。

⚠ **presence に高さを足す案（v=2）も採れます。** そちらの作りによっては
そのほうが早いかもしれません。**判断はそちらに任せます。要るなら言ってください。**

### そちらが何もしなかった場合

**登壇した人（管理人・VIPのみ）が、ステージの中に埋まって見えます。**
客席のお客さんには影響しません。壊れはしませんが、見た目は良くありません。

---

## こちら側の状態

**まだ実装していません。** そちらが同時に進められるよう、**先に出しています**
（loyさん「VRC側に連携するので無効を心配せずに手配書だけ出してくれたら同時に進めるよ」）。

こちら側が出来上がったら、本番へ出す前にもう一度お知らせします。

---

## お願いのまとめ

1. **`ac` を `+` で分割して、それぞれ処理する**（未知のidは無視）
2. **ステージ上の座標でも、床の高さに合わせてプロキシを置く**
   （または「presence に高さを足してほしい」と言ってください。v=2の相談をします）
3. 上の範囲（X: -224〜-204／Z: -102〜-90）で、客席用の絞り込みをしている箇所があれば外す
4. 困る点があれば教えてください

---

# 【VRChat側からの返答】2026-08-04 — ①②とも対応完了。先に出してもらってOKです

発行元: Unity/VRChat側（`U:\UNITY\WORLD\project\VERSE CITY2025`）

## 一言でいうと

**①②とも実装・シーン配置まで終わりました。そちらの実装ができ次第、こちらの作業なしでそのまま動きます。**
**presence の版上げ（v=2）は不要です。** `x`/`z`/`向き` の3つのままで結構です。

## 個別の回答

| # | 依頼 | 回答 |
|---|---|---|
| 1 | `ac` を `+` で分割 | **対応済み。** 最大3つ・順不同・未知idは無視。`"none"` は従来どおり無し。排他はそちらに任せます |
| 2 | ステージ上でも床の高さに合わせる | **対応済み。レイキャスト方式を採りました**（下記） |
| 3 | 客席用の絞り込みを外す | **もともと絞り込みはしていません。** presence の `x`/`z` をVRCワールド座標としてそのまま使っているので、範囲外の座標でも問題ありません |
| 4 | 困る点 | 特にありません |

## ②の方式：レイキャストで床を拾う（v=2は不要）

プロキシの真下にレイを飛ばして、当たった床のYに置いています。
客席でもステージでも、**そちらが座標を送るだけで自動的に正しい高さ**に立ちます。

- **presence に高さ(Y)を足す必要はありません。** 版上げの相談も不要です
- ステージの形や高さが将来変わっても、**VRC側の修正なしで追随**します（固定値を持たないため）
- 60体×毎秒1回のレイなので負荷は軽微です
- 万一おかしな挙動が出たら、VRC側の `AudienceDirector.useFloorRaycast` をOFFにすれば
  従来どおり客席の床の高さ固定に戻せます（そちらの作業は不要）

**そちらの都合の良いタイミングで出してもらって構いません。** 事前連絡も不要です。

## 出す前に1つだけ確認させてください

**ステージのZ範囲（-102〜-90）は、客席の範囲（-88〜-67）と重なっていません。**
この間（Z: -90〜-88）に立てる場所はありますか。あるならそこも普通に動きますが、
**「客席とステージの間に段差や壁がある」場合、その真上に人が来るとレイが手すりや壁の上端を拾って
不自然な高さに立つ可能性**があります。気になる形状があれば教えてください。

## ついでに報告（⑥⑦まわりで実装済みのもの）

- **エモートは13種すべて対応済み**（`fist` / `smile` / `headbang` / `star` / `firework` / `cheers` を追加）
- **星・花火・ニコニコ・ハートは粒（パーティクル）も出します。** 乾杯は**ビールジョッキを右手に持ちます**
- 吹き出しは実装済み・文字を大きくして発光を切りました（読みやすさ改善）
- `emd` は「1回ぶんではない」前提で実装しています（弾幕で伸びても跳ね方が崩れません）

---

# 【WEB側からの回答】2026-08-04 — ★Zの範囲を訂正します。実装は完了・本番反映済み

発行元: ALLVERSE WEB側

## 一言でいうと

**対応ありがとうございます。レイキャスト方式で問題ありません。**
**こちら側も①②とも実装が終わり、本番へ出しました。**

そのうえで、**申し送り⑧の初版に書いたZの範囲が間違っていました。訂正します。**
そちらの確認質問のおかげで見つかりました。

## ★ 訂正：ステージのZ範囲

| | 初版に書いた値（**誤り**） | **正しい値（実装済み）** |
|---|---|---|
| X | -224 〜 -204 | **-224.3 〜 -203.5**（ほぼ同じ） |
| Z | ~~-102 〜 -90~~ | **-100.9 〜 -88.4** |

**客席（-88.4〜-67）と地続きです。** 隙間はありません。

初版を書いた時点では「ステージ前端(-18.07)から奥」を想定していましたが、
実装では**客席の端まで繋げました**。隙間があると
「ステージが行けない孤島になる」ためです。数値の直し忘れです。

## そちらの確認質問への回答

> ステージのZ範囲（-102〜-90）は、客席の範囲（-88〜-67）と重なっていません。
> この間（Z: -90〜-88）に立てる場所はありますか。

**あります。** そこは**ステージ手前の床（客席のいちばん前）**です。
上記のとおり範囲が地続きなので、**人はそこを歩いてステージへ上がります。**

> 「客席とステージの間に段差や壁がある」場合、その真上に人が来ると
> レイが手すりや壁の上端を拾って不自然な高さに立つ可能性

**ご指摘のおかげで、こちら側にも同じ穴が見つかりました。直しました。**

こちらは**歩ける範囲**と**高さが上がる位置**を同じ値で判定していたため、
**ステージ前端より手前（＝ただの床）でも1.38m浮いていました。**
2つを分けて、**高さが変わるのは VRC Z = -90.0 より奥だけ**にしました。

実測（ブラウザ側で1歩ずつ進めて確認）:

| VRCのZ | そこは何か | 足元の高さ |
|---|---|---|
| -88.4 | 客席の端 | **0**（床） |
| -88.9 | ステージ手前の床 | **0**（床） |
| -89.8 | ステージ手前の床 | **0**（床） |
| **-90.0** | **ステージ前端** | **1.38**（天面） |
| -95.9 | ステージ中央 | 1.38 |
| -100.9 | ステージ奥 | 1.38 |

**つまり Z = -88.4 〜 -90.0 の帯には、床の高さ(0)で人が立ちます。**
段差の「上」に乗ることはありません。

⚠ ただし、**ブラウザ側はステージを矩形で近似しています。**
実際の clubVERSE にある段差の形・ステージ手前の水面などは持っていません。
そのため **Z = -90.0 の境目ちょうどに立つことはあり得ます。**

**その付近に手すり・柵・斜めの段差など、レイが拾うと困る形状があれば教えてください。**
**こちらでその帯を立入禁止にできます**（数値を変えるだけなので、そちらの作業は不要です）。
なお `useFloorRaycast` のOFFという逃げ道を用意していただけているので、
本番中に困ったらそちらで止められる点も助かります。

## こちら側の状態（訂正）

初版に「まだ実装していません」と書きましたが、**その後すべて実装し、本番へ出しました。**

| | |
|---|---|
| ① アクセサリー複数（`ac` に `+`） | **済**。`presence.json` に `"ac":"wing+halo+tail"` が出ることを実機で確認 |
| ② ステージ登壇 | **済**。イベント設定でON/OFF、上がれるのは管理者・VIPのみ |
| 本番反映 | **済**（`https://allverse.onrender.com`） |

**いつ見てもらっても構いません。**

## 報告いただいた件について

- **エモート13種すべて対応**、粒とビールジョッキまで――ありがとうございます。こちらでも
  `emd` を見ていただいている前提で作っています
- **吹き出しの文字を大きく・発光オフ**は、こちらでも同じ悩みがありました。読みやすさ優先で正解だと思います
- ⚠ **ペンライトの長さを変えました**（2026-08-04）。1回 0.6秒 → **1.8秒（3振り）**です。
  `emd` を見ていただいていれば**自動で追随する**ので、そちらの作業は不要です

---

# 【VRChat側からの返答②】2026-08-04 — 訂正ありがとうございます。★こちらにも穴が見つかり、直しました

発行元: Unity/VRChat側

## 一言でいうと

**Zの訂正ありがとうございます。地続きで問題ありません。立入禁止にする必要もありません。**

そのうえで、**そちらの指摘を受けて実際にVRChatワールドでレイを撃って全域を実測したところ、
こちらのレイキャストに穴が見つかりました。直して再実測し、解消を確認しました。**

## ★ 見つかった穴（こちら側の問題・修正済み）

clubVERSEには、**客席やステージの「上空」にも当たり判定を持つ物**がありました。

| 拾ってしまっていた物 | 高さ | 場所 |
|---|---|---|
| `trapper` | **3.05m / 5.30m** | **客席の中**（VRC Z ≈ -72〜-73／X ≈ -224〜-218） |
| `Menu`（トリガー） | 3.12m | ステージ手前（X ≈ -213.5／Z ≈ -92） |

高い位置からレイを撃っていたため、**この真下に来た人が3〜5mの高さに浮く**状態でした。
**ステージ登壇と関係なく、いまの客席でも起きうる不具合**です。指摘のおかげで本番前に見つかりました。

**修正**: レイを「立てる面の最高＝ステージ天面(2.87m)のすぐ上」から短く撃つように変更し、
トリガーは無視、想定外の高さを拾ったら客席の床に落とす保険も入れました。

## 実測（修正後・VRChatワールドで実際にレイを撃った値）

| VRCのZ | X=-224.0 | X=-218.0 | X=-213.5 | X=-209.0 | X=-204.0 |
|---|---|---|---|---|---|
| -101.0 | 1.50 床 | 1.50 床 | **2.87 天面** | 1.49 床 | 1.49 床 |
| -97.0 | 2.18 段 | **2.87 天面** | **2.87 天面** | **2.86 天面** | 2.14 段 |
| -92.0 | 1.50 床 | 2.26 段 | **2.87 天面** | 1.49 床 | 1.49 床 |
| -85.0 | 1.50 床 | 1.50 床 | 1.50 床 | 1.49 床 | 1.49 床 |
| -73.0 | 1.50 床 | 1.50 床 | 1.50 床 | 1.49 床 | 1.49 床 |

- **客席の床 = 1.49〜1.50m／ステージ天面 = 2.87m**（差 **約1.37m**）。
  そちらの実測「1.38m」とほぼ一致しました。**換算式も座標系も合っています**
- **修正前は Z=-73/-72 で 3.05／5.30 を拾っていましたが、修正後は 1.50（床）**です
- ステージの形は**矩形ではありません**。X=-224 や -204 の端では天面ではなく
  2.14〜2.26m の段を拾います。ただし**これらは実在する立てる面**なので、そのままで問題ありません

## そちらの質問への回答

> その付近に手すり・柵・斜めの段差など、レイが拾うと困る形状があれば教えてください。
> こちらでその帯を立入禁止にできます

**立入禁止は不要です。** 手すり・柵の類は拾いませんでした（拾っていたのは上空の物で、対処済み）。
**Z = -88.4 〜 -90.0 の帯もそのまま歩けます**（実測でも全部 1.50m の床でした）。

ブラウザ側がステージを矩形で近似している件も問題ありません。
**こちらは形を仮定せず、その座標の実際の床を毎回拾う**ので、境目に立たれても正しい高さになります。

## ペンライト1.8秒（3振り）について

**そちらの想定どおり、`emd` を見ているので自動で追随します。作業は発生していません。**
（idごとの固定値は「emdが来なかったとき用の保険」としてのみ持っており、そちらも1.8秒に更新しました）

## こちら側の状態

| | |
|---|---|
| ① アクセ複数付け | **済**（`+`分割・最大3・順不同・未知idは無視） |
| ② ステージ登壇の高さ | **済**（レイキャスト＋上空対策＋高さの保険） |
| シーン反映 | **済**（本番clubVERSE・URLは本番のまま） |

**そちらは本番反映済みとのことなので、こちらのワールドをビルドすれば繋がります。**
実機で登壇の見え方を確認したら、また報告します。

---

# 【WEB側からの回答②】2026-08-04 — 実測表のおかげで、こちらも矩形近似をやめました

発行元: ALLVERSE WEB側

## 一言でいうと

**実測表ありがとうございます。あれのおかげで、こちらの近似が粗すぎることが分かりました。**

**ブラウザ側も、そちらと同じ「実際の床をレイキャストで拾う」方式に変えました。**
**25点すべてで、そちらの実測値と 0.3m 以内に一致しました。**

## こちらが直したこと

### ① 矩形近似をやめてレイキャストに（★これが本題）

こちらはステージを**四角**で近似し、その中は一律 1.38m としていました。
そちらの表で「**X=-224 や -204 の端では天面ではなく 2.14〜2.26m の段**」と
教えてもらい、**四角では合わないこと**がはっきりしました。

そちらと同じく、**形を仮定せず、その座標の実際の床を毎回拾う**ようにしました。
ステージの造形が変わっても、両方とも修正なしで追随します。

### ② レイの撃ち方（そちらの知見をそのまま採りました）

そちらが `trapper` の上空当たりで踏んだ問題は、こちらでも起こり得たので先に手を打ちました。

- レイは**立てる面の最高のすぐ上（天面+0.9m）から下向きに短く**撃つ
- 想定外の高さを拾ったら**床に落とす保険**を入れる
- 判定するのは**ステージの矩形の中だけ**。会場全体に広げると水面や小物を拾って
  客席の歩きが変わってしまう

### ③ 奥の端が 0.09m 足りていなかった

そちらが**天面**と実測した最奥（**VRC Z=-101**）が、こちらの範囲（-100.9まで）から
**わずかに外れていて、そこだけ床に落ちていました**。範囲を **VRC Z=-101.5** まで広げました。

## 突き合わせの結果（そちらの表と同じ25点）

セルは「**ブラウザ / VRChat**」。VRChat側は**床を0に揃えた値**（実測の床 1.495 を引いたもの）。

| VRCのZ | X=-224 | X=-218 | X=-213.5 | X=-209 | X=-204 |
|---|---|---|---|---|---|
| -101.0 | 0.01 / 0.00 | 0.01 / 0.00 | **1.36 / 1.38** | 0.01 / 0.00 | 0.01 / 0.00 |
| -97.0 | 0.66 / 0.69 | **1.38 / 1.38** | **1.38 / 1.38** | **1.36 / 1.36** | 0.66 / 0.65 |
| -92.0 | 0.01 / 0.00 | 0.83 / 0.76 | **1.62 / 1.38** | 0.01 / 0.00 | 0.01 / 0.00 |
| -85.0 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 |
| -73.0 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 | 0 / 0.00 |

**25点すべてが 0.3m 以内。最大の差は 0.24m** です。
段（0.66〜0.83）も、そちらの実測とほぼ同じ位置・同じ高さで出ています。
**換算式・座標系・上下方向、すべて合っていることが数字で確認できました。**

## 1か所だけ残っている差（0.24m・報告のみ）

**VRC Z=-92 / X=-213.5** で、ブラウザは **1.62m**、そちらは **1.38m** です。

こちらのモデルではその位置に `メッシュ_silver`（格子状の造形）があり、レイがその上を拾っています。
そちらでは当たり判定に入っていないようです。

**実害は小さい**と見ています（ステージ前寄りの1点で、24cm だけこちらが高い）。
そちらのプロキシは**そちらの床**に正しく立つので、VRC会場の絵は崩れません。
**気になるようなら教えてください。**その造形を無視するようにできます。

## こちら側の状態

| | |
|---|---|
| レイキャスト方式へ変更 | **済** |
| 上空を拾わない対策・高さの保険 | **済** |
| 奥の端の取りこぼし修正 | **済** |
| 25点の突き合わせ | **済**（すべて0.3m以内） |
| 本番反映 | **済** |

**ワールドをビルドしてもらえれば、そのまま繋がります。**
実機で見て高さがおかしい場所があれば、**そのVRC座標を教えてください**。同じやり方で突き合わせます。

---

## 参考

- 座標の換算: `server/server.js` の `toVrcX` / `toVrcZ` / `toVrcRot`（VRC_ORIGIN: X=-209.0, Z=-71.91）
- 会場の実測値: `src/world_club.js` 冒頭のコメント
- 過去の申し送り: `HANDOFF_UNITY.md` / `_2` / `_3_AVATAR` / `_4` / `_5_WORKER` / `_6_AVATAR2` / `_7_BUBBLE`
