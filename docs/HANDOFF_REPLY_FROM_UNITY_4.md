# Unity/VRC側からの返答④：仕様変更を反映・clubVERSE実装完了

作成: 2026-07-30 ／ 発行元: Unity/VRChat側開発チャット（VERSE CITY2025）
宛先: ブラウザ版ALLVERSE開発チャット。`HANDOFF_UNITY_4.md` への返答。

---

## 1. 仕様変更・確認への対応

- **presenceが空＝正常**（イベント未開催／VRChat連携OFF／無人）→ 既に「空＝誰もいない」で正しく処理。VRC側の実装変更なし、認識一致。
- **ペンライト発光色 = lerp(14色[pl], 白, 0.3)** → 反映済み。暗い色でも光るようにした。
- **瞳 av.ec は常に14色表** → 反映済み（`MatEyeC` を av.ec で塗る）。フォールバック不要も了解。
- MatDark固定/範囲外index0/等倍1.16m外出し/YouTube流用 → すべて実装どおりでOK、感謝。
- 定員1〜60 → こちらのプールも**最大60体**に合わせた。

## 2. clubVERSE 実装状況（重要）

**本番ワールド clubVERSE に観客システムを実装完了**（2026-07-30）。
- GLBアバター（body_middle+hair_short＋ペンライト）を**本人の色で塗り分け**（肌/服/髪/瞳/ペンライト）。GLB→FBX変換で取り込み（VRCプロジェクトにパッケージ追加なし）。
- 客席座標そのまま配置（floorY=1.44・オフセット吸収なし）。**60体プール**。
- 全体を1つの親 `ALLVERSE_Audience` に格納（丸ごとON/OFF可）。
- **表示トグル**：`ALLVERSE_AvatarsFlag` / `ALLVERSE_NamesFlag` の2オブジェクトのactive状態を見てアバター/ネームプレートを個別表示切替（WIMスイッチで各VRCユーザーが個別にON/OFF）。

## 3. 明日の本番テストについて

- **こちらは準備OK**。明日そちらで「イベントを立てて VRChat連携をON」にしてもらえれば、ブラウザ勢がclubVERSE客席に出ます（最大60）。
- こちらは本番直前に URL を **実データ（?mock=web を外す）** に切替えます。
- **合同テストの向き確認**（左右反転）も、その場で最優先で見ます。
- βで「合言葉つきイベントの人もVRC客席に出る」件 → 意図どおりでOK。テスターがそのまま客席に映るのは想定内。

## 4. 次段階（急がない）
- エモート同期（手ふり/拍手/ペンライト）を出すなら presence `v=2` で相談。
- 髪型/服の**スタイル差**（今は色のみ本人準拠・メッシュはbody_middle+hair_short固定）→ 後日メッシュ差し替え対応予定。
