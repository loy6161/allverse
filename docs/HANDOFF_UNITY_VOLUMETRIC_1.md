# 立体伝送：ブラウザ側から Unity 側への申し送り①（2026-08-08）

宛先: `U:\UNITY\WORLD\project\VERSE CITY2025\Assets\ALLVERSE\_Research\VolumetricPoC\`
の担当（`SPEC_FRAME_FORMAT.md` を書いた側）

## 1. 作ったもの

**ブラウザ側の復号器を実装した**（仕様書 §9 に「まだ無い」と書かれていた3つ）。

`L:\企画用\WEB\verse_city_web\tools\volumetric\decoder.html`
（開発サーバー経由: `http://localhost:5179/tools/volumetric/decoder.html`）

- 画像／動画を読み込む口（ファイル選択・サンプル同梱）
- **識別ストリップ（128ブロック）の読み取り**
- タイルの切り詰め（RangeMin/Max）の逆算
- レイマーチによる再構成（WebGL2）
- 調整つまみ: 歩数／詰め直し／色の許容／枠を隠す／空白の境目／向きを補う
- 確認用の表示: `?mode=raw`（元の絵）／`?mode=depth`（5方向の深度を並べる）
- **レンジの手動指定**: `?range=minX,minY,minZ,maxX,maxY,maxZ`

## 2. 動いたところ

**識別ストリップは、もらった静止画から仕様どおりに読めた。**

| 項目 | frame1 | frame2 |
|---|---|---|
| sync | OK | OK |
| プロファイル | 4K | 4K |
| split | 1枚 | 1枚 |
| サブフレーム | 0 | 0 |
| シーケンス | 9 | 7 |
| RangeMin | −1, −1, −1 | −1, −1, −1 |
| RangeMax | 1, 1, 1 | 1, 1, 1 |

タイルの位置と向きの計算も合っている（深度タイルから人物のグレー値が読める）。

## 3. 動かなかったところと、その理由

**立体が1画素も出ない。**「5方向すべてが内側と言う点」が存在しない。

調べて分かったこと:

### ① ストリップのレンジが全域（−1〜1）になっている

仕様 §7 は「`Tight Bounds` が false でも切り詰めは働いている。前後が ±0.45 m ＝
volume 座標で 0.275〜0.725 なので、Left / Right のタイルは横方向が全体の約45%に
切り詰められている」と書いている。

**だが、もらったフレームのストリップには全域が載っている。**
ドライバが送信マテリアルの `_RangeMin/_RangeMax` だけ更新して、ストリップには
既定値が載った、という筋を疑っている。

### ② 実際のタイルを測ると、切り詰めが効いている形跡がある

深度タイルの中で人物が占める位置（タイル内の中心 x、0〜1）:

| タイル | 0 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| 中心x | 0.42 | 0.28 | 0.49 | 0.51 | 0.80 |
| 幅 | 319 | 89 | 104 | 104 | 153 |

⚠ **Front と Back は同じ x 軸を反対から見る**ので、中心は対称（0.42 なら 0.58）に
なるはず。実測は 0.28 で合わない。**タイルごとに違う切り詰めがかかっている**と考えるのが自然。

### ③ 送信側の「軸の反転」は、レンジが全域だと効かない

`ALLVERSE_FramePack.shader` の `rescale_depth`:

```
if (reversedAxis) { float t = 1.0 - hi; hi = 1.0 - lo; lo = t; }
```

反転は **lo と hi の入れ替え**で実装されている。レンジが全域（lo=0, hi=1）だと、
入れ替えても同じ値なので**何も起きない**。

そのため Back / Left / Top の深度が Front と同じ向きのまま届き、
Front から見た表面（q.z=+0.26）と Back から見た表面（q.z=−0.28）が交わらない
＝内側の点が存在しない。

受信側で反転を補うスイッチ（「向きを補う」）を付けて試したが、
①②が未解決なので、それだけでは出なかった。

## 4. お願いしたいこと

次のどれかがあれば、こちらで先へ進める。

1. **`FrameDriver` が動いた状態のフレーム**を1枚（ストリップのレンジが全域でないもの）
2. または、**もらった静止画を撮ったときの `_RangeMin` / `_RangeMax` の実値**
   （Unity の Inspector で `ALLVERSE_FramePack.mat` を見れば分かる）
3. できれば **位置合わせマークを OFF にしたフレーム**
   （仕様 §8-1 が自分で「ブラウザで復号を試すならまずこれを 0 にしたフレームで」と書いている）

## 5. こちらで確認したいこと（仕様への疑問）

- ストリップに載せるレンジは、**送信マテリアルに書くのと同じ値**か。
  同じであるべきだが、もらったフレームでは食い違っているように見える
- レンジが全域のとき、`reversedAxis` は**意図どおりに効いているか**。
  実装を読む限り効かないが、それで正しいのか（＝全域のフレームは想定外なのか）

## 6. 配信について（別件・ブラウザ側の都合）

ブラウザで立体に戻すには、**映像をピクセルとして読める**必要がある。

- **YouTube は不可**（別ドメインの映像は canvas に描けない）
- **TopazChat も不可**（VRChat内で受けるためのもので、ブラウザ再生を想定していない）
- 現実的なのは **Cloudflare R2 + HLS**（配信量が無料・CORS を自分で設定できる）
- ⚠ ただし R2 はカード登録が要る場合がある。**課金が絡むので、決める前に loyさんに相談する**

まずは **録画ファイル（mp4）を ALLVERSE のサーバーに置いて動かす**のが、
配信の決定を待たずに進められる道。同じ場所から配るので CORS の問題も起きない。
