# 申し送り: ALLVERSE（ブラウザ会場）→ PORTAL

発行: 2026-07-31 ／ 送り先: `L:\企画用\WEB\Allverse_Portal`（CODEX主担当）
件名: **イベントの動員記録を PORTAL に取り込みたい**

---

## 一言でいうと

ブラウザ会場（https://allverse.onrender.com）で、**イベントを立ててから閉じるまでの動員を
記録する機能を実装しました**。ピーク同接・累計人数・滞在時間が出ます。

これを PORTAL の記事・イベント総目録に載せたいので、**そちらから取りに来てもらう形**の
APIを用意しました。**そちらのコードはまだ何も変えていません。** 取り込むかどうか、
取り込むならどう入れるかを決めてください。

---

## なぜ「取りに来てもらう」形にしたか

方式は3案あり、**案1（こちらがAPIを出し、PORTAL側が取りに来る）**を採りました。

| 案 | 内容 | 判断 |
|---|---|---|
| **1（採用）** | こちらがAPIを公開し、PORTALが取りに来る | **本番Supabaseの書き込み鍵をRenderに置かなくて済む。** VRChat連携と同じ考え方 |
| 2 | こちらがSupabaseに直接書きに行く | PORTAL側は無改修で済むが、正本DBの書き込み権限を外に出すことになる |
| 3 | 両方 | 作るものが増える |

案2は「PORTALが何も変えなくていい」のが利点ですが、**ブラウザ会場は不特定多数が
繋ぐサービス**なので、そこに正本DBの書き込み鍵を置くのは避けました。
取り込みの工程（誰がSupabaseに入れるか）はそちらの都合で決めてください。

---

## APIの仕様

### エンドポイント

```
GET https://allverse.onrender.com/api/stats.json
Authorization: Bearer <STATS_TOKEN>
```

- **合言葉（`STATS_TOKEN`）が未設定の間、この口は開きません**（403）。使うと決まったら
  Render の環境変数に設定します。値は別途安全な経路で渡します
- CORSは許可済み（`Access-Control-Allow-Origin: *`）。ブラウザからでもサーバーからでも取れます
- クエリ: `?since=<UNIXミリ秒>`（それ以降に始まった開催だけ）／ `?limit=<件数>`（既定200）

### 返るもの

```jsonc
{
  "ok": true,
  "source": "allverse-web",
  "generatedAt": 1785439568054,
  "events": [
    {
      "runId": "evms7wp7sj-1785439568054", // 開催1回の識別子（これが主キー向き）
      "eventId": "evms7wp7sj",             // イベントの識別子
      "name": "WEEKENDS vol.12",
      "openedAt": 1785439568054,           // 立てた時刻（UNIXミリ秒・UTC）
      "closedAt": 1785443168054,           // 閉じた時刻。null なら開催中
      "live": false,
      "durationSec": 3600,

      "peak": 42,                          // ピーク同時接続
      "peakAt": 1785441000000,             // その時刻
      "unique": 87,                        // 累計ユニーク人数
      "entries": 103,                      // のべ入場回数
      "entriesLoggedIn": 61,               // うちログイン済み
      "entriesGuest": 42,                  // うちゲスト
      "nowInside": 0,                      // いま中にいる人数（開催中のみ意味を持つ）

      "avgConcurrent": 23.4,               // 平均同接（のべ滞在 ÷ 開催時間）
      "avgStaySec": 818,                   // 平均滞在
      "medianStaySec": 640,                // 滞在の中央値
      "maxStaySec": 3540                   // 最長滞在
    }
  ]
}
```

### 出さないもの

**訪問者ごとの行は返しません。** PORTALに必要なのは「ピーク・累計・滞在」であって
「誰が来たか」ではないと判断したためです。必要になったら相談してください。

メールアドレスもこちら側の記録に残していません（ハッシュ化しています）。

---

## 取り込み先の候補（そちらで判断してください）

こちらで見た限り、Supabaseの `events` テーブルに **`manual_metrics`（手入力の数値）** という
列があります。ここが自動化の受け皿になりそうに見えました。ただし
**PORTAL側の設計はそちらが正**なので、この見立ては参考程度に扱ってください。

決めてほしいのは次の3点です。

1. **取り込む工程を誰が持つか**（PORTALのビルド時に取りに来る／GitHub Actionsで定期取得／手動）
2. **どのテーブル・どの列に入れるか**（`manual_metrics` に混ぜるのか、専用の列/テーブルを足すのか）
3. **突き合わせのキー**。こちらの `runId` と PORTAL のイベントをどう対応づけるか
   （名前一致は事故るので、PORTAL側にこちらの `runId` を持つ列を足すのが安全だと思います）

---

## 注意点

- **開催中も取れます**（`live: true`）。値は途中経過なので、記事に載せるのは
  `closedAt` が入ってからにするのが安全です
- Render無料プランは15分アクセスが無いと寝ます。取りに来たとき初回だけ数十秒待たされることがあります
- 記録のズレは**最大1分**（サーバー再起動時の補正の粒度）
- **こちらの累計は「同じブラウザ・同じGoogleアカウントを1人」として数えています。**
  ブラウザを変えた同一人物は別人として数えられます。記事に書くときの表現に注意してください
- **断り書きは設置済み**（2026-08-02 更新）。入場前に「イベントでの記録が保存されます」と出しています。
  PORTALで数字を公開するなら、そちらの規約・プライバシーポリシーとも整合を取ってください

---

## loyall app との紐づけ（2026-08-02 追記・**設計はこれから**）

loyさんの要望:

> PORTAL連携した際に、loyall app側のユーザーデータとも紐づけたい。

**loyall app とは**（2026-08-02 本人談）:
> PORTALのデータのもとになる、XやVRC、YouTubeのデータを収集するApp群。App_Devで開発してるはず。
> この開発プロジェクトの初期に作ったものなので、まだHubに記録として残す仕組みになる前だったかも。

### 突き合わせに使えるもの（こちら側が持っている値）

| キー | 中身 | 使いどころ |
|---|---|---|
| メールのハッシュ | `u:` ＋ sha256(メールアドレス) の先頭16桁 | **loyall app 側もGoogleアカウントで人を識別しているなら**、同じ計算で一致させられる。メールそのものをやり取りせずに繋がる |
| 表示名 | Googleアカウントの表示名 | 補助。同名がありうるので単独では使わない |
| （将来）YouTubeチャンネルID | フェイズ2の 2c で取得予定 | **loyall app はYouTubeのデータを集めているので、こちらの方が本命かもしれない** |

### 調査結果（2026-08-02・App_Dev の実物を確認）

**loyall app はGoogleアカウント（メール）で人を識別していない。**
Supabaseに **`identities`（🪪名寄せ台帳）** があり、1人＝1行で各アカウントを束ねている。

| 列 | 中身 |
|---|---|
| `person_name` | その人の名前（軸） |
| `vrc_names` | VRChatの名前（複数可） |
| `x_handles` | Xのアカウント（複数可） |
| `yt_channel_ids` | YouTubeの **@ハンドル**（複数可） |

出典: `App_Dev/apps/loyall/Loyall_Data/src/pages/People.jsx` と `src/cache.js`（Supabaseへ直書き）

**→ こちらのメールのハッシュは鍵にならない。繋ぐなら YouTubeチャンネル。**

```
ALLVERSE WEBの参加者 ──[2cの合言葉方式]──▶ YouTubeチャンネル ──▶ identities.yt_channel_ids
```

⚠ **形式が違う点に注意。** `identities` が持つのは **@ハンドル**（`@loyloy` の形）だが、
YouTubeチャットから取れるのは **UC...形式のID**。変換が要る。
ただし loyall app 側に `migrations/bake-yt-channels.mjs`（ハンドルからチャンネル情報を引く処理）が
既にあるので、そこに相乗りできる。

### PORTALのログインで本人に紐づけてもらう案（2026-08-02 loyさん）

> PORTALでのログインができるようになったら、そこで各自に各SNSを紐づけてもらうようにしたらいいかもね。

**こちらの方が筋がよい。** 理由は2つ。

1. `identities` はいま**運営が手作業で埋めている**（🪪名寄せ画面）。
   本人に登録してもらえれば**手間が減り、精度も上がる**
2. **紐づけの置き場所が1つになる**。PORTALが「人の正本」になれば、
   ALLVERSE WEB も loyall app もそこを見るだけで済む

2cの合言葉方式は「ALLVERSE WEBだけで完結する」利点があるので、**両立させるのが現実的**。
PORTALで登録済みならそれを使い、無ければ合言葉で照合する。

**次にやること**: PORTALのログインが動き出したら、`identities` との関係
（人の正本をどちらに置くか）を設計する。

## こちら側の状態

- **本番デプロイ済み**（2026-08-02）。自動テスト・実機確認とも完了
- **`STATS_TOKEN` の設定はまだ＝この口はまだ閉じています**（PORTAL側の方針が決まってから開けます）
- 原本の仕様は `verse_city_web/docs/SPEC.md`「イベントの記録」節
- ⚠ 断り書きは**汎用的な文面に変更済み**（2026-08-02）。
  「イベントでの記録（入退場や発言など）が保存されます」と入場前に出しています
