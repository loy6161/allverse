import { createConceptAvatar } from './concept_factory.js';
export const STYLE_INFO = { name: 'Neon Beat', desc: '音楽空間で映える、発光アクセント付きローポリ。', badge: 'NEW' };
export const createStyleAvatar = (config) => createConceptAvatar('neon', config);
